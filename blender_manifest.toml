schema_version = "1.0.0"

id = "blender_vnf_exporter_extension"
version = "1.0.0"
name = "VNF Export Extension"
tagline = "Export blender mesh vnf to openscad"
maintainer = "runerback"
type = "add-on"

website = "https://github.com/runerback/blender-vnf-exporter-extension"
tags = ["Import-Export"]

blender_version_min = "4.2.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

platforms = ["windows-x64", "linux-x64"]

[permissions]
files = "Export .scad to disk"
